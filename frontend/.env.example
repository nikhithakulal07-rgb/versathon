# LearnQuest Frontend Environment Variables
# NOTE: The frontend connects ONLY through the local Vite proxy (/api) to the backend.
# NO API KEYS, NO SECRETS, NO AI CREDENTIALS EXIST IN THE FRONTEND.
# All requests are securely routed to the backend API.
